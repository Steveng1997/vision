export interface cobros {

  // id?: number;
  // idDocument?: string;
  // piso1?: string;
  // piso2?: string;
  // terapeuta?: string;
  // encargada?: string;
  // otro?: string;

  efectivo: boolean;
  bizum: boolean;
  tarjeta: boolean;
  transaction: boolean;

  // nota?: string;
}
